# Deep-learning
## LR 1

При подборе значения `NUM_EPOCHS` возникли сложности, так как обучение модели при ограничении по CPU занимает значительно больше времени.

В репозитории присутствует Python-скрипт (`.py`), который полностью повторяет логику Jupyter Notebook, но с исправленной проблемой многопоточной загрузки данных.

Возможно позже я доработаю модель и сделаю лучше.